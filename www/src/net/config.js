// ─────────────────────────────────────────────────────────────────────────
//  SERVER ADDRESS — paste your server URL between the quotes below, e.g.
//
//     export const SERVER_URL = 'https://play.example.com';
//     export const SERVER_URL = 'https://203.0.113.7:8787';
//
//  Leave it '' and the game runs in fully offline mode (no leaderboards,
//  no sync — everything else works). Note: when the game is served over
//  https (GitHub Pages), browsers require the server to be https too.
// ─────────────────────────────────────────────────────────────────────────
export const SERVER_URL = '';

export const PROTOCOL = 1;
